{
    "configurations": [
        {
            "name": "Containers: Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/okazje-plus/package.json",
                "localRoot": "${workspaceFolder}/okazje-plus"
            }
        }
    ]
}